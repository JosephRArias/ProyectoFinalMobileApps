<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ProyectoFinalMobile.Views.RestaurantsDetailsPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Near By" Command="{Binding getNearBySearchCommand}" HorizontalOptions="CenterAndExpand"
                VerticalOptions="CenterAndExpand"/>
            <ListView ItemsSource="{Binding Restaurants}"
                  x:Name="listv">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Id}"/>
                                <Label Text="{Binding Name}"/>
                                <Label Text="{Binding Photo}"/>
                                <Label Text="{Binding Scope}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>